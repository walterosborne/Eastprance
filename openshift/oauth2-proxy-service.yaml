apiVersion: v1
kind: Service
metadata:
  name: oauth2-proxy
  namespace: qmi-scorecard-dev
  labels:
    app: oauth2-proxy

spec:
  selector:
    app: oauth2-proxy

  ports:
    - name: oauth2-proxy
      port: 4180
      targetPort: oauth2-proxy
      protocol: TCP

  type: ClusterIP